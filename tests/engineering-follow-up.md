Continue this engineering investigation with a focused independent-review correction. Complete the following work autonomously, update a concise plan, and update the canvas as the corrected outputs arrive.

1. The original h=0.04 m result gives tip deflection 0.8779 m on a 2 m span. That is about 44% of the span and outside a credible small-displacement interpretation. Preserve those original outputs as formal linear-model results, label this limitation explicitly, and rerun all three depths with 100 N total load (rather than 10,000 N), retaining the same E, A and geometry. You may use the existing per_run nodes/edges to avoid rebuilding geometry, but recompute the system or independently verify load scaling. Save NEW versions of structure.json and engineering-results.json; keep the exact structure schema used previously. Include deflection/span, maximum axial strain, reaction residual normalized by force norm, and relative energy discrepancy for each depth. Do not invent an engineering safety threshold. Explain the scope of these dimensionless diagnostics and the absence of buckling/yield/nonlinear checks.
2. The report claimed its proposed tight residual tolerances were observed, yet the shallow-case residual norm was around 1.89e-7 N. Replace that blanket assertion with the ACTUAL absolute and normalized residuals. Distinguish the model equations passing numerical checks from the physical design being validated. Correct the work-energy label: strain energy U = 0.5*u^T*F under gradually applied linear loading; don't confuse this with u^T*F.
3. Fix and improve interactive.html: its log-scale formula produces an invalid logarithm at zero load. Make scale=0, 0.5, 1, and 3 all render valid finite points and correctly scaled numeric values. Add an accessible label for the slider, responsive SVG/viewBox, units, clear axes, and plot the corrected low-load results. Use a linear y-axis if that makes zero handling clearer. Test this logic before presenting it.
4. Update the depth/deflection PNG, material-volume graph if needed, and engineering-report.md with the corrected results and explicit comparison to the original high-load case. Include an independent review note in the report. Present the corrected graphs, interactive.html, and the existing truss GLB together on the canvas. The geometry and force-sign coloring of the GLB can be reused because the load is a positive scaling.

Keep computation under the container deadline. Use existing artifacts through inputArtifactIds. Finish the actual corrections and deliver the files; do not end by asking whether to continue.
